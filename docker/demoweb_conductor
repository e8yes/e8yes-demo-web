FROM demoweb_base:latest

# Runs the cluster conductor.
WORKDIR /home

EXPOSE 1026

ENV LD_LIBRARY_PATH="./bin/"

CMD ["./bin/conductor_main", \
     "--replication_config_path=/host/home/cluster_conductor.pbtxt"]
