version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:11
    steps:
      - checkout
      - run:
          name: Current directory
          command: ls
      - run:
          name: Run maven test
          command: make test
